# CNN Integration for Black-Box Optimization (BBO)\n\nThis document explains how Convolutional Neural Networks (CNNs) can be integrated into the BBO optimization pipeline to enhance performance and provide alternative approaches to Gaussian Process-based surrogate modeling.\n\n## CNN Approaches for BBO\n\n### 1. **CNN Surrogate Models** (`cnn_surrogate.py`)\n\n**Purpose**: Replace or supplement Gaussian Processes with deep learning-based function approximation.\n\n**Key Features**:\n- **Deep surrogate modeling** with uncertainty quantification\n- **Monte Carlo dropout** for uncertainty estimation\n- **Adaptive architecture** based on function dimensionality\n- **Expected Improvement** acquisition with CNN predictions\n\n**When to Use**:\n- ✅ High-dimensional functions (4D+)\n- ✅ Large training datasets (25+ evaluations)\n- ✅ Non-stationary or complex function landscapes\n- ✅ When GP kernel assumptions may not hold\n\n**Usage Example**:\n```python\nfrom bbo.cnn_surrogate import CNNBayesianOptimizer, propose_next_point_cnn\n\n# Fit CNN surrogate model\nX, y = function_evaluations  # Your data\nn_candidates = 10000\n\n# Propose next point using CNN\nx_next, report = propose_next_point_cnn(\n    X, y, \n    acquisition=\"ei\", \n    xi=0.01,\n    seed=42,\n    n_candidates=n_candidates\n)\n\nprint(f\"Next point: {x_next}\")\nprint(f\"Model type: {report['model_type']}\")\nprint(f\"EI score: {report['ei_score']}\")\n```\n\n### 2. **CNN Function Landscape Modeling** (`cnn_landscape.py`)\n\n**Purpose**: Model optimization landscapes as spatial images, learning acquisition functions directly from landscape patterns.\n\n**Key Features**:\n- **2D landscape visualization** and spatial modeling\n- **Encoder-decoder architecture** for acquisition function learning\n- **Spatial smoothness regularization** for realistic acquisition maps\n- **Direct acquisition function prediction** from landscape images\n\n**When to Use**:\n- ✅ **2D functions only** (current implementation)\n- ✅ Visual/spatial function landscapes\n- ✅ Learning from multiple optimization campaigns\n- ✅ Transfer learning across similar functions\n\n**Usage Example**:\n```python\nfrom bbo.cnn_landscape import CNNAcquisitionOptimizer\n\n# Train on historical optimization data\nX_history = [run1_inputs, run2_inputs, ...]  # Multiple 2D optimization runs\ny_history = [run1_outputs, run2_outputs, ...]\n\ncnn_acq = CNNAcquisitionOptimizer(grid_size=64)\ncnn_acq.fit(X_history, y_history, epochs=100)\n\n# Predict next point for new function\nX_current, y_current = current_evaluations  # Current 2D function data\nx_next, report = cnn_acq.predict_next_point(X_current, y_current)\n\nprint(f\"CNN landscape prediction: {x_next}\")\n```\n\n### 3. **Hybrid CNN-GP Pipeline** (`cnn_integration.py`)\n\n**Purpose**: Combine CNN and GP approaches for robust ensemble optimization.\n\n**Key Features**:\n- **Ensemble weighting** between different model types\n- **Adaptive model selection** based on problem characteristics\n- **Fallback mechanisms** for robust operation\n- **Performance tracking** for continuous improvement\n\n**Ensemble Modes**:\n\n**a) Weighted Ensemble**:\n```python\nfrom bbo.cnn_integration import HybridCNNGPOptimizer\n\n# Configure ensemble weights\nensemble_weights = {\n    \"gp\": 0.6,                 # 60% GP\n    \"cnn_surrogate\": 0.4       # 40% CNN surrogate\n}\n\noptimizer = HybridCNNGPOptimizer(ensemble_weights)\nx_next, report = optimizer.propose_hybrid_point(\n    X, y, \n    acquisition=\"ei\",\n    xi=0.01,\n    func_idx=function_index\n)\n```\n\n**b) Adaptive Selection**:\n```python\nfrom bbo.cnn_integration import AdaptiveCNNGPPipeline\n\npipeline = AdaptiveCNNGPPipeline()\n\n# Automatically selects best model based on:\n# - Function dimensionality\n# - Number of evaluations  \n# - Recent performance history\nselected_model = pipeline.select_best_model(\n    func_idx=0, \n    dim=function_dimension,\n    n_points=num_evaluations,\n    recent_improvements=improvement_history\n)\n```\n\n### 4. **Full Pipeline Integration**\n\nReplace the standard BBO pipeline with CNN-enhanced version:\n\n```python\nfrom bbo.cnn_integration import run_cnn_enhanced_optimization\nfrom pathlib import Path\n\n# CNN-enhanced optimization run\nresult = run_cnn_enhanced_optimization(\n    initial_dir=Path(\"data/initial_data\"),\n    weekly_dir=Path(\"data/weekly\"),\n    use_hybrid=True,  # Enable hybrid CNN-GP\n    cnn_ensemble_weights={\n        \"gp\": 0.7,\n        \"cnn_surrogate\": 0.3\n    }\n)\n\nprint(f\"Week {result['week_k']} optimization complete\")\nprint(f\"CNN enhanced: {result['cnn_enhanced']}\")\nprint(f\"Models used: {result['diagnostics_summary']['unique_models']}\")\n```\n\n## CNN vs GP Comparison\n\n| Aspect | Gaussian Process | CNN Surrogate | CNN Landscape |\n|--------|-----------------|---------------|---------------|\n| **Dimensionality** | All dimensions | 3D+ optimal | 2D only |\n| **Data Requirement** | 5+ points | 25+ points | Multiple campaigns |\n| **Uncertainty** | Analytical | Monte Carlo | Spatial patterns |\n| **Training Speed** | Fast | Moderate | Slow |\n| **Inference Speed** | Fast | Fast | Fast |\n| **Interpretability** | High | Low | Visual |\n| **Transfer Learning** | Limited | Possible | Strong |\n\n## Implementation Strategy\n\n### **Week 6+ Integration**\n\nIntegrate CNN capabilities with existing Week 6 advanced optimization features:\n\n```python\n# In strategy.py - enhanced parameter tuning\ndef adaptive_exploration_params_cnn(week: int, y_hist: np.ndarray, \n                                   func_idx: int, model_type: str) -> Dict:\n    \"\"\"Enhanced parameter tuning with CNN model awareness\"\"\"\n    base_params = adaptive_exploration_params(week, y_hist, func_idx)\n    \n    # CNN-specific parameter adjustments\n    if model_type == \"cnn_surrogate\":\n        # CNNs may need higher exploration due to different uncertainty\n        base_params[\"xi\"] *= 1.2\n        base_params[\"uncertainty_factor\"] *= 1.1\n    elif model_type == \"cnn_landscape\":\n        # Spatial acquisition functions may be smoother\n        base_params[\"xi\"] *= 0.9\n    \n    return base_params\n```\n\n### **Recommended Usage Pattern**\n\n1. **Start with GP** (Weeks 1-3): Build initial dataset\n2. **Introduce CNN surrogate** (Week 4+): For 4D+ functions with 25+ points\n3. **Add CNN landscape** (Week 5+): For 2D functions with visualization needs\n4. **Full hybrid ensemble** (Week 6+): Combine all approaches with intelligent weighting\n\n### **Performance Monitoring**\n\n```python\n# Track model performance for adaptive selection\ndef update_model_performance(func_idx: int, model_type: str, \n                           y_old: float, y_new: float):\n    \"\"\"Update performance tracking for model selection\"\"\"\n    improvement = y_new - y_old\n    \n    # Store in performance history\n    performance_db[func_idx][model_type].append({\n        \"improvement\": improvement,\n        \"timestamp\": time.time(),\n        \"week\": current_week\n    })\n    \n    # Adaptive weight adjustment\n    if improvement > threshold:\n        increase_model_weight(model_type, func_idx)\n    else:\n        decrease_model_weight(model_type, func_idx)\n```\n\n## Installation and Dependencies\n\nTo use CNN features, install additional dependencies:\n\n```bash\n# Install PyTorch for CNN models\npip install torch torchvision\n\n# Install additional visualization dependencies\npip install matplotlib scipy\n\n# For landscape interpolation\npip install scipy\n```\n\n## Visualization and Analysis\n\n### CNN Training Progress\n```python\n# Monitor CNN surrogate training\ncnn_opt = CNNBayesianOptimizer(input_dim=4)\nfit_info = cnn_opt.fit(X, y)\n\nprint(f\"Training loss: {fit_info['final_loss']:.6f}\")\nprint(f\"Convergence: {fit_info['avg_loss']:.6f}\")\n```\n\n### Landscape Visualization\n```python\n# Visualize 2D function landscapes and acquisition functions\ncnn_acq = CNNAcquisitionOptimizer()\ncnn_acq.visualize_acquisition(\n    X_2d, y_2d, \n    save_path=\"artifacts/cnn_landscape_week6.png\"\n)\n```\n\n### Model Comparison\n```python\n# Compare GP vs CNN predictions\nfrom bbo.model_comparison import compare_surrogate_models\n\ncomparison_results = compare_surrogate_models(\n    X_train, y_train, X_test, y_test,\n    models=[\"gp\", \"cnn_surrogate\", \"cnn_landscape\"]\n)\n\nprint(f\"GP RMSE: {comparison_results['gp']['rmse']:.4f}\")\nprint(f\"CNN Surrogate RMSE: {comparison_results['cnn_surrogate']['rmse']:.4f}\")\n```\n\n## Benefits of CNN Integration\n\n### **1. Enhanced Modeling Capability**\n- **Non-parametric function approximation** without kernel assumptions\n- **Scalability to high dimensions** without exponential complexity\n- **Transfer learning** across similar optimization problems\n\n### **2. Improved Uncertainty Quantification**\n- **Monte Carlo dropout** for robust uncertainty estimation\n- **Ensemble uncertainty** from multiple CNN predictions\n- **Spatial uncertainty patterns** from landscape modeling\n\n### **3. Advanced Acquisition Strategies**\n- **Learned acquisition functions** from optimization campaigns\n- **Spatial pattern recognition** in acquisition landscapes\n- **Multi-objective integration** through ensemble weighting\n\n### **4. Visualization and Interpretability**\n- **2D landscape visualization** for spatial understanding\n- **Acquisition heatmaps** for decision interpretation\n- **Training progress monitoring** for model validation\n\n## Integration with Week 6 Advanced Features\n\nCNN capabilities integrate seamlessly with existing Week 6 advanced optimization:\n\n```python\n# Enhanced Week 6 with CNN integration\nfrom bbo.strategy import adaptive_exploration_params\nfrom bbo.cnn_integration import HybridCNNGPOptimizer\n\n# Week 6+ optimization with CNN enhancement\ndef week6_cnn_enhanced_optimization(X, y, func_idx, week):\n    # Advanced parameter tuning (existing Week 6 feature)\n    advanced_params = adaptive_exploration_params(week, y, func_idx)\n    \n    # CNN-enhanced optimization\n    optimizer = HybridCNNGPOptimizer({\n        \"gp\": 0.6,\n        \"cnn_surrogate\": 0.4\n    })\n    \n    # Multi-objective portfolio balancing\n    portfolio_weights = multi_objective_portfolio_balance(all_performances)\n    \n    # Enhanced uncertainty quantification with CNN\n    x_next, report = optimizer.propose_hybrid_point(\n        X, y,\n        acquisition=\"ei\",\n        xi=advanced_params[\"xi\"],\n        beta=advanced_params[\"beta\"],\n        func_idx=func_idx\n    )\n    \n    return x_next, {\n        **report,\n        \"week6_advanced\": True,\n        \"cnn_enhanced\": True,\n        **advanced_params\n    }\n```\n\n**CNN integration provides a powerful extension to the existing BBO framework, offering enhanced modeling capabilities, improved uncertainty quantification, and advanced acquisition strategies that complement the sophisticated Week 6 optimization features.**